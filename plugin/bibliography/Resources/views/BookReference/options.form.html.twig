{% extends "ClarolineCoreBundle:Administration:layout.html.twig" %}

{% block title %}
    {{ parent() ~ ' - ' ~ 'innova_audio_recorder_config' | trans({}, 'innova_audio_recorder') | striptags | raw }}
{% endblock %}

{% block breadcrumb %}
    {{
        macros.breadcrumbs([
            {
                'name': 'administration'|trans({}, 'platform'),
                'href': path('claro_admin_index')
            },
            {
                'name': 'packages'|trans({}, 'platform'),
                'href': path('claro_admin_plugins')
            },
            {
                'name': 'icap_book_reference_config'|trans({}, 'icap_bibliography'),
                'href': ''
            }
        ])
    }}
{% endblock %}


{% block section_content %}
    {{ parent() }}
    <div class="panel-heading">
        <h3 class="panel-title">{{ 'icap_book_reference_config'|trans({}, 'icap_bibliography') }}</h3>
    </div>

    <div class="panel-body">
        {{ form_start(form, {'action': path('bibliography_config_save', {'id':id }) , 'method': 'POST'}) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
